# build output
dist/
.astro/

# dependencies
node_modules/

# secrets / local env
.env
.env.local

# os
.DS_Store

# archives
*.zip

# TinaCMS
# The admin app is rebuilt on every deploy — no need to commit it.
public/admin
# but the generated schema in tina/__generated__ MUST be committed so Tina
# Cloud can index the branch. Only the local cache is ignored.
tina/__generated__/.cache
